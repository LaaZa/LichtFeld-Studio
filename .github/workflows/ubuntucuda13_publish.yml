name: Ubuntu CUDA 13 Build

on:
  workflow_dispatch:

# Required to allow the action to publish to GitHub Releases
permissions:
  contents: write

jobs:
  build-ubuntu:
    runs-on: ubuntu-26.04
    container:
      image: nvidia/cuda:13.3.1-devel-ubuntu26.04
    strategy:
      fail-fast: false
      matrix:
        build_type: [Release]
    env:
      VCPKG_ROOT: ${{ github.workspace }}/vcpkg
      CC: gcc-14
      CXX: g++-14
      VCPKG_BUILD_TYPE: release
      VCPKG_FORCE_SYSTEM_BINARIES: 1
      CUDA_HOME: /usr/local/cuda
      CUDAToolkit_ROOT: /usr/local/cuda
      CUDAToolkit_LIBRARY_ROOT: /usr/local/cuda

    steps:
      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y ca-certificates gpg wget curl git unzip tar pkg-config

          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ noble main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
          apt-get update

          # Added `file` and `desktop-file-utils` which are required by linuxdeploy
          apt-get install -y \
            cmake gcc-14 g++-14 ccache ninja-build \
            zip python3 python3-dev python3-venv libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev \
            libwayland-dev libxkbcommon-dev libegl-dev libdecor-0-dev libibus-1.0-dev libdbus-1-dev \
            libsystemd-dev libgtk-3-dev \
            nasm autoconf autoconf-archive automake libtool \
            file desktop-file-utils

          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100 \
                              --slave /usr/bin/g++ g++ /usr/bin/g++-14

      - name: Configure environment variables
        run: |
          echo "/usr/bin" >> $GITHUB_PATH
          echo "/usr/local/cuda/bin" >> $GITHUB_PATH
          echo "CMAKE_MAKE_PROGRAM=/usr/bin/ninja" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=/usr/local/cuda/lib64:${LD_LIBRARY_PATH}" >> $GITHUB_ENV

      - name: Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Free disk space
        run: |
          apt-get clean
          rm -rf /var/lib/apt/lists/*
          rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache /usr/local/lib/android /opt/az
          rm -rf /usr/local/cuda/nsight* /usr/local/cuda/compute-sanitizer
          rm -rf /usr/local/cuda/extras /usr/local/cuda/tools
          rm -rf /usr/local/cuda/doc
          find /usr/local/cuda/targets/x86_64-linux/lib -name '*.a' \
            ! -name 'libcudart_static.a' \
            ! -name 'libcudadevrt.a' \
            ! -name 'libnvjpeg_static.a' \
            ! -name 'libnvjpeg2k_static.a' \
            ! -name 'libculibos.a' \
            -delete 2>/dev/null || true
          df -h

      - name: Cache vcpkg tool
        id: cache-vcpkg-tool
        uses: actions/cache@v4
        with:
          path: ${{ env.VCPKG_ROOT }}
          key: vcpkg-tool-${{ runner.os }}-2026-02-21
          restore-keys: vcpkg-tool-${{ runner.os }}-

      - name: Setup vcpkg
        run: |
          if [ ! -f "${{ env.VCPKG_ROOT }}/vcpkg" ]; then
            echo "vcpkg binary missing, bootstrapping..."
            git clone https://github.com/microsoft/vcpkg.git ${{ env.VCPKG_ROOT }} \vert{}\vert{} true${{ env.VCPKG_ROOT }}/bootstrap-vcpkg.sh -disableMetrics
          fi

      - name: Configure vcpkg triplet
        run: |
          cat >> ${{ env.VCPKG_ROOT }}/triplets/x64-linux.cmake << 'EOF'
          set(VCPKG_BUILD_TYPE release)
          set(VCPKG_MAX_CONCURRENCY 2)
          EOF

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/ccache
          key: ccache-${{ runner.os }}-${{ matrix.build_type }}-${{ github.run_id }}
          restore-keys: ccache-${{ runner.os }}-${{ matrix.build_type }}-

      - name: Cache vcpkg packages
        uses: actions/cache@v4
        with:
          path: /github/home/.cache/vcpkg/archives
          key: vcpkg-pkgs-${{ runner.os }}-linux-gui-prebuilt-ort-v1-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', '.github/overlays/**') }}
          restore-keys: vcpkg-pkgs-${{ runner.os }}-linux-gui-prebuilt-ort-

      - name: Configure
        run: |
          cmake -B build -S . -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCMAKE_CUDA_ARCHITECTURES="86;89;90" \
            -DCUDAToolkit_ROOT=/usr/local/cuda \
            -DCUDAToolkit_LIBRARY_ROOT=/usr/local/cuda \
            -DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.VCPKG_ROOT }}/scripts/buildsystems/vcpkg.cmake \
            -DBUILD_FORMAT_TESTS=OFF \
            -DBUILD_TESTS=OFF \
            -DBUILD_PYTHON_STUBS=OFF \
            -DLFS_DEV_IMPORT_SOURCE_PYTHON=OFF \
            -DLFS_DEV_IMPORT_SOURCE_RESOURCES=OFF \
            -DLFS_ENABLE_CUDA_DEVICE_DEBUG=OFF \
            -DCMAKE_C_FLAGS="-pipe" \
            -DCMAKE_CXX_FLAGS="-pipe"

      - name: Clean vcpkg buildtrees
        run: |
          rm -rf build/vcpkg_installed/vcpkg/blds build/vcpkg_installed/vcpkg/pkgs
          rm -rf "${{ env.VCPKG_ROOT }}"/buildtrees "${{ env.VCPKG_ROOT }}"/downloads "${{ env.VCPKG_ROOT }}"/packages

      - name: Build
        run: cmake --build build -j $(nproc)

      - name: Prepare install payload
        run: |
          DESTDIR=dist cmake --install build

      - name: Package Raw Artifact Tarball
        run: |
          # Package the raw bin/libs before linuxdeploy pulls in system dependencies
          cd dist
          tar -czvf ../LichtFeld-Studio-Ubuntu26.04-CUDA13.4.tar.gz usr/
          cd ..

      - name: Build AppImage
        run: |
          export APPIMAGE_EXTRACT_AND_RUN=1
          
          # Download linuxdeploy
          wget -q https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage
          
          # Find the main executable
          EXE_PATH=$(find dist/usr/bin -type f -executable | head -n 1)
          if [ -z "$EXE_PATH" ]; then
            echo "Error: No executable found in dist/usr/bin"
            exit 1
          fi
          EXE_NAME=$(basename "$EXE_PATH")
          
          # Use existing Desktop file, or create a temporary one if none was installed
          DESKTOP_FILE=$(find dist -name "*.desktop" | head -n 1)
          if [ -z "$DESKTOP_FILE" ]; then
            DESKTOP_FILE="dist/${EXE_NAME}.desktop"
            cat > "$DESKTOP_FILE" <<EOF
          [Desktop Entry]
          Type=Application
          Name=LichtFeld Studio
          Exec=$EXE_NAME
          Icon=$EXE_NAME
          Categories=Graphics;
          EOF
          fi
          
          # Use existing icon, or create a temporary SVG if none was installed
          ICON_FILE=$(find dist -name "*.png" -o -name "*.svg" | head -n 1)
          if [ -z "$ICON_FILE" ]; then
            ICON_FILE="dist/${EXE_NAME}.svg"
            cat > "$ICON_FILE" <<EOF
          <svg width="256" height="256" xmlns="http://www.w3.org/2000/svg">
            <rect width="256" height="256" fill="#333333"/>
            <text x="128" y="140" font-family="Arial" font-size="48" text-anchor="middle" fill="#ffffff">LFS</text>
          </svg>
          EOF
          fi
          
          # Build AppImage (this automatically fetches required shared libraries into dist/usr/lib)
          ./linuxdeploy-x86_64.AppImage \
            --appdir dist \
            --executable "$EXE_PATH" \
            --desktop-file "$DESKTOP_FILE" \
            --icon-file "$ICON_FILE" \
            --output appimage
          
          # Rename output
          mv *.AppImage LichtFeld-Studio-Ubuntu26.04-CUDA13.4.AppImage

      - name: Upload Build Artifact (Actions Tab)
        uses: actions/upload-artifact@v4
        with:
          name: LichtFeld-Studio-Ubuntu2604-CUDA13.4
          path: dist/usr/
          retention-days: 14

      - name: Publish to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          # Groups manual runs into unique releases based on their run ID
          tag_name: build-${{ github.run_id }}
          name: "LichtFeld Studio Build #${{ github.run_number }}"
          files: |
            LichtFeld-Studio-Ubuntu26.04-CUDA13.4.AppImage
            LichtFeld-Studio-Ubuntu26.04-CUDA13.4.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
